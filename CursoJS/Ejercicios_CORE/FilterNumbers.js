// Filtar los números de la lista que sean mayores a 3
//     Definir el número con el que se va a comparar cada item de la lista
//     Hacer una nueva lista con los números que sean más grandes

const lst = [1,2,3,4,5,6]
const greater_than_3 = lst.filter(i => i > 3)
console.log(greater_than_3)